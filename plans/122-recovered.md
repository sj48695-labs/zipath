# #122 자동 복구 계획

> plan 에이전트가 파일 대신 출력한 구조화 계획을 DevLoop가 복구했습니다.

## 원본 계획

[09:50:45] plan #122 시작 (model: gpt-5.6-terra)
기존 PR #155의 헤드에는 이미 #122 플랜(P1–P7 완료)이 있습니다. 현재 `develop`에는 기능 구현이 반영되어 있고, PR #155에만 남은 변경은 P7의 Playwright 회귀 보강입니다.

제안: 기존 플랜을 갱신해 P8로 “PR #155 헤드의 E2E 변경을 현재 #122 작업 브랜치에 재연결하고, 즉시 상태·10초 안내·45초 timeout·오류 분류·동일 조건 재시도를 검증”을 추가하겠습니다. 새 브랜치나 PR은 만들지 않습니다.

1. 기존 플랜 유지
2. P8 복구 phase를 추가해 수정
3. 중단

## 실행 체크리스트

### Phase 1 (완료)

- [ ] 원본 계획의 P1 범위를 구현하고 검증한다.

### Phase 7 (완료)

- [ ] 원본 계획의 P7 범위를 구현하고 검증한다.

### Phase 8 (완료): PR #155 Playwright 회귀 검증 재연결

- PR #155 head에만 남아 있던 `request-feedback.spec.ts` 변경을 현재 브랜치에 복구한다.
- 청약 및 실거래가 요청 직후 `role="status"` 진행 안내, 10초 후 콜드 스타트 안내,
  45초 timeout 구현과 오류 분류, 동일 조건 재시도를 Playwright route mock으로 검증한다.
- UI의 “참고용이며 법적 효력 없음” 고지를 회귀 테스트로 유지한다.

### Phase 9 (완료): 45초 timeout 오류 분류 Playwright 검증

- Playwright 가상 시간으로 청약과 실거래가 요청의 10초 콜드 스타트 안내 및
  45초 timeout을 빠르게 재현한다.
- timeout이 HTTP 오류가 아닌 Render 준비 안내로 분류되는지 확인한다.
